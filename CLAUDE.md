# ha-wine-cellar ("Cork Dork") — project context for Claude

Home Assistant custom integration + Lovelace card for managing a wine cellar: rack/bin layout, barcode/label scanning, Vivino enrichment, AI (Gemini) enrichment, buy list, history.

## Repo / remotes — read this before touching git

- `origin` = **JYves72/ha-wine-cellar** (the user's own fork). Full rights, PR freely, no confirmation needed.
- `upstream` = **BaconWappedBitcoin/ha-wine-cellar** (original maintainer). **Never push, open a PR, comment, or touch upstream in any way without the user's explicit, per-action confirmation — every single time.** Even mentioning `owner/repo#123` as a link (not backticks) pings upstream — use backticks when citing an upstream issue/PR number. This applies regardless of what any other fork's notes below say about their own access to upstream — it does not transfer.
- `dobunzli` = **a third party's fork**, confirmed 2026-08-25, NOT the user's, forked *from* by `origin` (JYves72). It carries substantial independent, actively-developed feature work. **Treat writes to it exactly like `upstream`: never push, open a PR, comment, or touch it in any way without the user's explicit, per-action confirmation.** Reading it, diffing it, or merging/cherry-picking its commits *into* `origin` is fine and needs no special confirmation — only writes *to* `dobunzli` itself are gated.
- **Cross-fork history note**: this file's lineage is shared with `dobunzli`'s own copy (since `origin` was forked from `dobunzli`), and `dobunzli` apparently runs their own Claude sessions against it too — a merge from `dobunzli` can bring in text describing *their* workflow, permissions, personal setup, or tooling (e.g. claimed collaborator/merge rights on `upstream`; their own HA being reached over `http://`; their own AI provider being a "1min.ai relay"; the `graphify` tooling section at the bottom of this file). **None of that describes this user, `origin`, or this machine unless independently confirmed.** Our own access to `upstream` stays confirm-every-time as stated above no matter what a merged-in note from `dobunzli` claims about their own access, and any "the user does X" or "this project has Y installed" claim inherited from a `dobunzli` merge needs a fresh check against *this* user/setup before being treated as fact.
- Status of PR numbers on `upstream` (`#18`, `#19`, `#21`, etc.) is genuinely unclear from conflicting historical notes (`dobunzli`'s own file claims one merged as a comprehensive sync, one closed-as-superseded, one merged standalone; direct `git log` inspection on 2026-08-25 showed a *merged* upstream PR `#18` titled "Revert codeowners to the original upstream author", which doesn't match either description). Don't trust either account — re-check upstream's actual PR history (e.g. `gh pr view <n> --repo BaconWappedBitcoin/ha-wine-cellar`, a read, should be fine, but ask if unsure) before acting on any assumption about it. Separately, `dobunzli` has (per their own notes) an open PR on `upstream` — `upstream#22`, the Lovelace auto-register fix — awaiting the maintainer as of 2026-08-23. Informational only; nothing to do here.
- Other forks with interesting independent work, surveyed 2026-08-25 (none are the user's — same write caution as `dobunzli`/`upstream`): `los7702/ha-wine-cellar` — two-way Vivino account sync, **merged into `origin` 2026-08-27** (see "Current state" below; the initial survey estimate of ~15 commits was low, the actual merged branch `vivino-enrich-thin-records` carried 34 commits). Still unexplored/not imported: `karticus/ha-wine-cellar` (branch `codex/kl_integration`: K&L Wine Merchants newsletter/PDF import via Gemini), `jamespreid/ha-wine-cellar` (small fixes incl. `FlowResult`→`ConfigFlowResult` for HA 2024.4+ compat), `mangysaurus/ha-wine-cellar` (batch label upload, Gemini vision model fallback, capacity/slot calc fix, backup/restore hardened to use authenticated websocket calls). `vindigator/ha-wine-cellar` had nothing ahead of upstream at last check.

## Current state (updated 2026-08-27 — dobunzli catch-up + los7702 Vivino sync now merged)

`origin` has been caught up to `dobunzli/main` via three staged merges checkpointed at their release tags:

- **v2.12.0** (PR #19–#27: inventory search/filters, backup/restore+CSV rework, storage UX + enrichment tracking, logical arrangement suggestion, iOS camera diagnostics, arrangement-analysis feature, mobile search bar fix, Lovelace auto-register fix) — merged and tested clean on this user's Synology, 2026-08-25.
- **v2.13.0** (PR #28–#30, adds Vivino-on-label + parallel lookups) — merged; tested on this user's Synology 2026-08-25 with no issues reported.
- **v2.14.2** (PR #31–#40) — merged and tested clean on this user's actual setup (Raspberry Pi 4 running HA, Synology used only for backups), 2026-08-25/26.

`dobunzli`'s own notes on v2.14.2 (useful context, confirmed on *their* instance 2026-08-23): PR #32 fixes rack-shrinking stranding bottles in slots that no longer exist; **PR #33 changes how photos are stored — they move out of the wine records and onto disk at `<config>/wine_cellar_photos/` on first start, with the wine keeping a served URL instead of an inline payload** (backups still carry photos inline by design, so both old and new backups restore fine) — worth knowing before restarting HA on this checkpoint, since it changes what's on disk. PR #35 was two rounds of code-review fixes; PR #37 then audited what #35 had skipped and found real bugs, the worst being Vivino's HTML scrape mangling accented characters (storing "Château" instead of "Château"). Reviewing was three rounds deep by the end, and each round found bugs in the previous round's fixes — treat one review pass as a start, not a verdict, if reviewing further work here.

- **v2.15.0** (merged 2026-08-27): `los7702/vivino-enrich-thin-records` (34 commits) — two-way Vivino account sync. Adds `vivino_account.py` (`VivinoAccountClient`: `async_verify`, `async_get_cellar`, `async_change_bottles`, `async_get_wishlist`, `async_get_my_wines`, thin-record enrichment) and `vivino_reconcile.py` (three-way reconcile: baseline / Vivino-side state / local state), new options-flow fields (`vivino_cellar_url`, `vivino_session_cookie`, `vivino_auto_sync`), a `sync_vivino` service, a `has_vivino_account` capability flag, and five new `WineCellarStorage` methods (`get_vivino_baseline`, `set_vivino_baseline`, `remove_vivino_bottles`, `set_vivino_pending_push`, `set_vivino_sync_status` — all confirmed present post-merge). This is the same client that was earlier found sitting **untracked** in a working directory (see git history above this point) — it depended on `vivino_reconcile.py` and those five storage methods, neither of which existed yet at the time; both now do.
  - This merge conflicted in 7 files with `dobunzli`'s AI-provider abstraction work, purely because both features happened to touch the same structural spot in shared files (`config_flow.py`'s imports/schema/validation slot, `const.py`'s tail, `translations/en.json`'s `data`/`error` blocks, `websocket.py`'s `get_capabilities`, `wine-cellar-card.ts`'s state/capResult) for unrelated reasons — none were genuine either/or conflicts. Resolved additively: both features fully coexist (`_async_validate_ai_provider` and `_async_validate_vivino` both run in `async_step_init`, AI first then Vivino). The only thing dropped was los7702's bare `"gemini_api_key"` schema field, which duplicated the AI-provider abstraction's own `CONF_GEMINI_API_KEY` field (same underlying key `"gemini_api_key"` — keeping both would have been a duplicate dict key). Compiled JS bundle conflicts resolved by rebuild (`npm run build`), never by picking a side. `FRONTEND_VERSION` bumped to `20260827a`, `manifest.json` version to `2.15.0`.
  - **Tested against this user's real Vivino account on their Raspberry Pi, 2026-08-27 — works as expected** (config validation, the 🔄 Vivino Sync button, cellar import into Unassigned). Not yet pushed to `origin/main` on GitHub as of this note — still local commits only.

## Key technical findings from recent work (don't re-discover these)

- **Vivino's `www.vivino.com/api/explore/explore` search API silently ignores the `q` param** for unauthenticated requests — returns the same generic "trending wines" list regardless of query (verified live, repeatedly). `search_wine()` in `vivino.py` now checks the top result for basic relevance before trusting it, falling back to HTML scraping (which *does* do real text search) when it looks wrong.
- **`api.vivino.com` (the mobile-app-facing backend) is open and unprotected** — no auth, no special headers, plain `GET https://api.vivino.com/wines/{id}` / `/vintages/{id}` / `/grapes/{id}` / `/foods` all work. Used for reliable refreshes once a wine's `vivino_id` is known (`VivinoClient.get_wine_by_id`). It has **no search endpoint** (by-id lookup only) and **no price data anywhere** — confirmed via extensive live testing (explore API, HTML scrape, real browser navigation, mobile API, even `curl` with browser-identical headers). Vivino's real price is not reliably scrapable; AI estimation (with user consent) is the fallback.
- A `Vivino Batch failed` bug turned out to be a websocket schema validation error (`ai_fallback` param sent by the frontend but not declared in the backend schema) — always check HA logs (`Paramètres > Système > Journaux`) for exact errors before guessing.
- LWIN / Liv-ex was investigated as a wine-ID/pricing source and **rejected**: the free LWIN database has no price/rating data (identification only), and Liv-ex's price data (Wine Matcher, Automation) is a professional fine-wine-trade product, not viable for this project.
- **Vivino's real cellar sync (reading/writing the user's own cellar) is possible via `www.vivino.com`'s Inertia.js endpoint using a pasted session cookie** — from `los7702`'s work, merged into this repo as of v2.15.0 (see "Current state" above). Untested against a real account so far.
- **iOS Safari blocks the live camera when the page is reached over `http://` (not a secure context) — confirmed on `dobunzli`'s own setup; worth checking whether this user's HA is also reached over plain http.** Not a secure context ⇒ Safari does not expose `navigator.mediaDevices` at all ⇒ `getUserMedia` throws a `TypeError`. No code fix exists; `label-camera.ts` detects it up front and offers the file input, which carries `capture="environment"` and opens the native camera with no secure context needed. If this doesn't reproduce for this user, it's because they're on https — good, nothing to do.
- **Camera errors must be told apart by `err.name`, never `err.message`.** Safari's denial message is "The request is not allowed by the user agent or the platform in the current context" — it contains neither "NotAllowed" nor "Permission", so substring matching on the message sends every iOS failure to the generic branch. Shared helpers live in `frontend-src/src/utils/camera.ts`.
- **Barcode scanning cannot work on iOS at all**: WebKit does not implement `BarcodeDetector`. Unrelated to the http/https question — even over https it reports "not supported on this browser". Unblocking it needs a WASM decoder (zxing) — ask this user whether that dependency is worth adding before spending time on it.
- **`hass.data["lovelace_resources"]` does not exist and never has** — that wrong key is why card auto-registration could silently do nothing, which is upstream issue #1 ("Custom element not found"). The collection is at `hass.data[LOVELACE_DATA]` (a `LovelaceData` dataclass, `.resources`) on current HA, or a plain `hass.data["lovelace"]` dict on older ones. Two traps around it: `async_items()` does **not** load the store (`ResourceStorageCollection` defers until a write), so an existence check reads empty and duplicates the resource on every restart — call `_async_ensure_loaded()` first; and YAML-mode dashboards have no `async_create_item` at all, so registration is impossible there by design.
- **Which add path calls what** (measured by `dobunzli` 2026-08-22, rebalanced in PR #29): barcode hit ⇒ Vivino only; barcode miss ⇒ falls through to the label; label photo ⇒ AI, *plus* a non-blocking Vivino lookup fired after the details step opens; search by name ⇒ Vivino only. Before #29 the label path never touched Vivino at all, so photo-added wines had no rating and **no `vivino_id`**, which also barred them from the cheap by-id refresh. The background enrichment only ever fills gaps and never overwrites the AI's reading, the user's photo, or name/winery/vintage — and it checks the candidate really is the same wine, because Vivino answers every query with something.
- **Vivino latency, measured by `dobunzli` with production headers**: the explore API takes 1.1–1.7 s and returns ~910 KB for five results; grape lookups are 0.06–0.2 s each. AI label recognition is a single call, no retry, 45 s timeout — that wait is entirely the AI provider's (check what this user's own provider/relay is if optimizing this path), so there is nothing to optimise on our side beyond not lengthening it. Requests that are independent now run concurrently (`search_wine`'s explore + HTML, `lookup_barcode`'s two barcode DBs, grape names). **`fetch_extras=False` must stay sequential** — that flag exists so batch refresh does not double its request volume across the whole cellar.
- **Occupancy and placement math is shared, not re-derived**: `frontend-src/src/utils/location.ts` owns the notion of a *container* (a bin, a box, the bottom zone, or one grid slot with its depth) plus usage, labels, next-free-depth and multi-bottle planning. `utils/suggest.ts` (where a new bottle should go) and `utils/arrange.ts` (what is untidy) both build on it. It previously existed in four diverging private copies.

## Deploy workflow (Synology, no CI)

- No CI/CD, no remote dev environment. Home Assistant itself runs on a **Raspberry Pi 4**, not the Synology — the Synology NAS is used for backups only. The user copies changed files to the Pi's config directory over a **Samba network share** (`\\homeassistant.local\config\` or `\\<pi-ip>\config\`), sometimes browsed via Synology's File Station as a client for that share (hence past notes saying "Synology File Station" — that described the browser, not where the files physically live). Anything under `<config>/` in these notes — including `custom_components/wine_cellar/`, and the `wine_cellar_photos/` folder PR #33 introduced — lives on that same share, at the Pi's config root. After any change: list the exact changed `.py` files + compiled frontend JS to copy over, then do a **full Home Assistant restart** (a simple integration reload is NOT enough — Python module caching).
- Frontend: TypeScript/Lit in `frontend-src/src/`, built via `npm run build` (run from `frontend-src/`) to `custom_components/wine_cellar/frontend/wine-cellar-card.js`. After any frontend change, bump `FRONTEND_VERSION` in `custom_components/wine_cellar/const.py` (format `YYYYMMDD` + incrementing letter) to bust the browser cache.
- **HACS-based testing — corrected 2026-08-27: the user's HACS custom repository was tracking `BaconWappedBitcoin/ha-wine-cellar` (`upstream`), NOT `dobunzli`.** An earlier note here got this wrong; the user confirmed they never pulled from `dobunzli`'s fork by any route. The real problem was that `dobunzli`'s PRs landed on `upstream`'s `main` without `upstream` cutting a matching GitHub Release, so HACS (which only looks at Releases, never raw commits) silently stayed on a stale version relative to what was actually in `upstream/main`.
- **Decision 2026-08-27: switching the HACS custom repository to track `origin` (`JYves72/ha-wine-cellar`) instead of `upstream`.** `origin` now has a strict superset of `upstream`: the full `dobunzli` catch-up (v2.12.0–v2.14.2, tested on this user's own Raspberry Pi) plus `los7702`'s two-way Vivino account sync (v2.15.0, tested working against this user's real Vivino account 2026-08-27) — a feature that exists on neither `upstream` nor `dobunzli`. This also fixes the PR/release-drift problem structurally, since the user now controls when releases are cut on the repo HACS tracks. Steps: `git push origin main`, `gh release create v2.15.0 --target main ...`, then in HACS remove the `BaconWappedBitcoin` custom repository and add `JYves72/ha-wine-cellar` (category Integration) in its place.
- HACS's "update available" detection is driven by GitHub Releases, not raw commits on `main` — after merging a PR to `main` (on whichever repo is actually being tracked, now `origin`), bump `manifest.json`'s `version` (semver: patch for fixes, minor for features) and `gh release create vX.Y.Z --target main` on that commit, or HACS will keep resolving to the last-tagged release and silently miss the fix. Skip the version bump for no-op/doc-only changes (e.g. a LICENSE-only PR) unless asked otherwise.
- **After every deploy (Synology copy *or* HACS redownload) + full HA restart, also remind the user to hard-refresh the browser tab (Cmd+Shift+R).** Home Assistant's frontend is a persistent single-page app — restarting the backend does NOT reload an already-open tab's JS. Two consecutive "the fix still doesn't work" reports on this project turned out to be exactly this: the fix was correct both times, the browser tab just hadn't picked it up yet. Don't let "restart Home Assistant" alone imply the browser will pick up new code.
- Verification (no live HA access from here): `npm run build` **should now be warning-free** — the 4 long-standing `TS2339` warnings on `BarcodeLookupResult` in `add-wine-dialog.ts` came from that type not declaring fields the dialog reads, and it was completed as part of the review passes above; **a new warning from this point on is a real finding, not background noise.** Also: `node --check` on the compiled JS bundle, `python -c "import ast; ast.parse(...)"` per changed `.py` file (Windows: use `python`, not `python3` — the latter resolves to a Microsoft Store stub unless disabled under Settings > Apps > Advanced app settings > App execution aliases).
- **Local frontend preview**: `custom_components/wine_cellar/frontend/index.html` is a standalone preview page with a full mock `hass` object (mocked `callWS` for every command the card uses — wines, cabinets, buy list, history, settings, etc. — no real backend needed). `.claude/launch.json` defines `preview-server` (`python3 -m http.server 5051` in the frontend dir) which serves it directly against the last `npm run build`; `frontend-dev` (`npm run dev`, port 5050) is rollup watch only. **The preview caches hard: `location.reload()` will happily re-run the previous bundle.** Use `navigate` with `force: true` after a rebuild, and check something you just changed is actually present before concluding a fix does not work. Open it to interact with the *real* compiled component — including real mouse drag-and-drop — before asking the user to test on Synology/HACS. Includes a dedicated "Capacity Test" cabinet (a Bulk Bin already at capacity + a Spare Bin) for exercising the bin/box capacity-check logic specifically. Prefer this over writing a throwaway test harness from scratch. For a case the mock doesn't cover cleanly, a synthetic DOM `drop` event (with a hand-built `dataTransfer.getData("text/plain")` payload matching what `cabinet-grid.ts`'s real drag produces) is a reliable way to exercise `_onWineDrop` deterministically without fighting mouse-coordinate drag simulation.
- **A repo-local PreToolUse hook blocks `cat`/`head`/`tail`/`sed`/`awk` in Bash and the Read tool without `offset`/`limit` for files >150 lines.** Use `Read` with `offset`/`limit`, or plain `grep` (careful: piping `grep` into `head`/`tail` in the *same* command also trips the hook).
- **This repo may be checked out in more than one place** (confirmed 2026-08-25: an SSH-connected Home Assistant checkout, and a local Windows copy) — don't assume they stay in sync; re-check `git log --oneline -3 main` on whichever checkout you're using before relying on its state. A network-mapped drive (`Z:`) was also seen for this repo but couldn't be read reliably by tooling (stat failures) — prefer a local-disk checkout when one is available.

## Branch / PR workflow

One branch per feature/fix, tested by the user on Synology, then PR + merge on the fork. **Branch from `main`, not from wherever you happen to be standing** — starting a second feature without checking out `main` first silently stacks it on the previous unmerged branch (happened once; fixed with `git rebase --onto main <old-base> <branch>`, plus a bundle rebuild, since the compiled JS then contained code the branch's sources no longer had).

**Several branches open at once is a deploy problem, not just a review one**: each carries its own compiled bundle and its own `FRONTEND_VERSION`, and the user can only run one on the Synology. When they pile up, merge them and hand over a single rebuilt bundle from `main` rather than asking for several deploys. Resolve the inevitable `const.py` / bundle conflicts by **rebuilding**, never by taking one side — a bundle picked from one side no longer matches its sources. After pushing a merge commit, GitHub needs ~10–20s to recompute mergeability; `gh pr merge` fails with "not mergeable: the merge commit cannot be cleanly created" until it does. Wait and retry rather than believing it. For genuinely large sessions, group related changes into a small number of themed PRs rather than one-per-tiny-fix (discussed and agreed with the user) — but if PRs are stacked (PR B based on PR A's branch), **do not delete branch A when merging PR A** (`gh pr merge --delete-branch`) until PR B has been retargeted/merged — deleting a stacked PR's base branch auto-closes it on GitHub. Retarget or merge in the right order instead.

## Where to look

- `custom_components/wine_cellar/vivino.py` — Vivino explore API, HTML scrape, mobile API (`api.vivino.com`), barcode lookup.
- `custom_components/wine_cellar/gemini.py` — AI (Gemini/OpenAI-compatible) enrichment, label recognition, wine list extraction.
- `custom_components/wine_cellar/websocket.py` — all frontend↔backend commands; most business logic (refresh, batch refresh, settings, backup/restore) lives here.
- `custom_components/wine_cellar/wine_storage.py` — persisted data shape (`.storage/wine_cellar`), the Wine/Cabinet/BuyList schema, plus (since v2.15.0) the Vivino-baseline/pending-push/sync-status methods.
- `custom_components/wine_cellar/vivino_account.py` — two-way Vivino account sync client (session-cookie based, reads/writes the user's real Vivino cellar — distinct from `vivino.py`'s public search/lookup).
- `custom_components/wine_cellar/vivino_reconcile.py` — three-way reconcile logic (baseline / Vivino state / local state) backing the sync.
- `docs/vivino-import.md` — user-facing instructions for obtaining the Vivino session cookie and cellar URL this feature needs.
- `frontend-src/src/wine-cellar-card.ts` — main card (grid, tabs, batch actions, stats).
- `frontend-src/src/components/` — dialogs (wine detail, add wine, inventory, rack settings, Vivino/AI settings, wine list scan, arrangement report).
- `frontend-src/src/utils/` — shared logic, extracted so it stops being copied: `location.ts` (containers, occupancy, placement), `suggest.ts` (where a new bottle should go), `arrange.ts` (what is untidy), `search.ts` (`normalizeText`, filtering, sorting), `camera.ts` (why the camera failed).

## Persistent memory

There's also a Claude auto-memory system (outside this repo) with the upstream-confirmation rule and deploy workflow — this file is the repo-visible, git-tracked counterpart so the context survives even in a fresh environment/session that doesn't have that memory loaded.

## graphify (inherited from `dobunzli`'s merge — not set up for `origin`/this user)

**This whole section describes `dobunzli`'s own tooling on their own Mac, not anything installed or requested for this fork.** Kept for reference in case this user ever wants to try it themselves; skip it otherwise, and don't treat "this project has a knowledge graph" as true for this checkout unless `graphify-out/graph.json` actually exists here.

This project has a knowledge graph at graphify-out/ with god nodes, community structure, and cross-file relationships.

Rules:
- For codebase questions, first run `graphify query "<question>"` when graphify-out/graph.json exists. Use `graphify path "<A>" "<B>"` for relationships and `graphify explain "<concept>"` for focused concepts. These return a scoped subgraph, usually much smaller than GRAPH_REPORT.md or raw grep output.
- If graphify-out/wiki/index.md exists, use it for broad navigation instead of raw source browsing.
- Read graphify-out/GRAPH_REPORT.md only for broad architecture review or when query/path/explain do not surface enough context.
- After modifying code, run `graphify update .` to keep the graph current (AST-only, no API cost).

**Not installed everywhere.** graphify lives on `dobunzli`'s main Mac only; `~/.claude/` is per-machine and Synology Drive skips dot-folders, so nothing above travels with the repo except this file. On a machine where `graphify` is missing — or where `graphify-out/` is absent — the rules above point at a tool that is not there. Set it up first, then rebuild the graph:

```
pipx install graphifyy      # note the double y; plain "graphify" is not on PyPI
graphify claude install     # writes this section + two PreToolUse hooks
graphify update .           # ~6s, no LLM calls
```

**What it is actually good for** (measured by `dobunzli` on their checkout, not assumed): `graphify god-nodes` is accurate — it named `WineCellarCard`, `Wine`, `InventoryDialog` and `WineCellarStorage` as the hubs, matching what hand exploration found. `graphify query` is much weaker: asked how photos are stored it put `photos.py` first, then listed twenty unrelated nodes and truncated at "355 more nodes cut". Treat it as a **structural index, not an answering engine** — use it to orient ("where does this mechanism live"), and read the files directly to diagnose or fix, where precision matters more than orientation.

Note the gemini large-file hook and graphify's hooks coexist deliberately: Claude Code runs every matching `PreToolUse` hook and any one of them can deny. The gemini hook is registered globally (absolute path, covers every project); graphify's are registered per-project. Do not merge them into one file — that is what caused the duplicate-firing this setup was cleaned up from.
